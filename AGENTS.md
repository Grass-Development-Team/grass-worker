# AGENTS

## 工作前置

1. 开始任何任务前，先读取 `.gitignore` 和 `.git/info/exclude`，确认哪些内容不能提交。
2. `superpowers` 相关文档永远不要提交，尤其是 `docs/superpowers/`。
3. `docs/agents/` 里的代理对话/草稿文档也不要提交。

## 计划文档

1. 所有计划文档统一放在 `docs/plans/`。
2. 写计划时优先产出“单个最小功能”的计划；总计划只负责拆分阶段和顺序，不直接混做实现。

## 分支规则

1. 不要使用 `worktree`。
2. 每个新功能单独开一个新分支。
3. 分支命名统一使用 `<type>/<content>`，除 `main` 和 `dev` 外都遵守，例如 `feat/implement-register`。

## 会话粒度

1. 每一轮会话只完成一个最小功能。
2. 功能拆分必须足够细，保证单轮内可以设计、实现、测试、回顾。
3. 不要在同一轮里同时推进用户、团队、订阅、配额、部署等多个独立子系统。
