{
  "name": "grass-frontend",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bunx vite --config vite.config.ts",
    "build": "bunx vite build --config vite.config.ts",
    "test": "bun test"
  },
  "devDependencies": {
    "typescript": "latest",
    "vite": "latest"
  }
}
